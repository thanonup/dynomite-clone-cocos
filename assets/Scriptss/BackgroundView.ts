import { _decorator, Color, Component, debug, Graphics, Node } from 'cc'
const { ccclass, property } = _decorator

@ccclass('BackgroundView')
export class BackgroundView extends Component {
    start() {}

    update(deltaTime: number) {}
}
